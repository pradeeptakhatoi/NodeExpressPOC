<div class="d-flex flex-column flex-md-row align-items-center p-3 px-md-4 mb-3 bg-white border-bottom box-shadow">
    <h5 class="my-0 mr-md-auto font-weight-normal">
        <a href="/">Node Express POC</a>
    </h5>
    {{!-- {{#if authenticated}}
    <p class="my-0 mr-md-auto font-weight-normal">Welcome, {{user.name}}</p>
    {{/if}} --}}
    <nav class="my-2 my-md-0 mr-md-3">
        <a class="p-2 text-dark" href="/tasks">Tasks</a>
        <a class="p-2 text-dark" href="/contactus">Contact Us</a>
        <a class="p-2 text-dark" href="/aboutus">About Us</a>
    </nav>
    {{#if authenticated}}
    <form method="POST" action="/logout">
        <button class="btn btn-outline-primary">Logout</button>
    </form>
    {{else}}
    <a class="btn btn-outline-primary" href="/login">Login</a>
    {{/if}}
</div>